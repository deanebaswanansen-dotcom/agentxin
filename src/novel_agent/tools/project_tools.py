from novel_agent.providers.base import ToolDescriptor

ProjectLoadTool = ToolDescriptor(name="ProjectLoadTool", purpose="Load novel project files.")
ContextBuildTool = ToolDescriptor(name="ContextBuildTool", purpose="Build model context from project files.")
