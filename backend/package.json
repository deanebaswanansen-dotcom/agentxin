{
  "name": "novel-writing-agent-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend (Fastify + TypeScript) for the Novel Writing Agent",
  "type": "module",
  "scripts": {
    "cli": "tsx src/cli/main.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads --maxWorkers=1 --minWorkers=1 --fileParallelism=false",
    "acceptance:long-novel": "tsx scripts/run-long-novel-acceptance.ts",
    "acceptance:continuity": "tsx scripts/run-continuity-acceptance.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "fastify": "5.11.3"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "fast-check": "3.23.1",
    "tsx": "4.23.12",
    "typescript": "5.6.3",
    "vitest": "3.2.6"
  }
}
