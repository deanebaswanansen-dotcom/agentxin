# 小说创作智能体 (Novel Agent) 架构重构与改进规格说明书 (Spec)

## 1. 背景与现状

经过端到端的实际测试与功能评估，现有的 `agentxin` 项目虽然集成了大模型进行小说生成的各个环节（创意、大纲、写作、审查），但在产品定义、交互体验和系统架构上存在严重缺陷。当前系统被降级为“被动的 API 批处理脚本”，而非真正的自主智能体（Agent）。

本 Spec 文档旨在梳理当前存在的问题，并定义下一代“多合一小说创作智能体”的重构标准与架构规范。

## 2. 核心架构问题与重构规范

### 2.1 统一工具链，消除技术栈冗余
**当前问题**：系统中同时平行存在 Node.js CLI（`backend/src/cli/main.ts`）与 Python CLI（`src/novel_agent/cli.py`）两套实现，逻辑重合度高，状态不互通，维护成本极大。
**重构规范**：
- **明确单一核心**：应当将 LangGraph（Python 端）作为 Agent 的工作流引擎核心，而将 Node.js 降级为单纯的 Web/API 服务层，或者完全收敛到单一技术栈（例如全 Python 或全 TypeScript 的 Agent 框架）。
- **统一指令入口**：废除双 CLI，建立唯一的项目级命令行入口（如 `novel-agent` 命令），确保所有交互和底层状态管理使用同一套标准。

### 2.2 状态驱动与自主性（Active Agent Workflow）
**当前问题**：系统缺乏上下文感知能力。用户必须手动输入 `idea`、`outline`、`write`、`check` 推进流程。
**重构规范**：
- **状态机与生命周期**：Agent 需维护全局小说项目的生命周期（Project State）。引入事件驱动架构，在用户或模型完成某一章节初稿后，Agent 应当在后台**自动**触发审查（Review）或连贯性检查任务。
- **主动建议机制**：Agent 应在闲置或读取到用户设定更改时，主动生成并在 UI 层面抛出建议（如“检测到世界观变动，是否更新大纲？”），实现从“被动执行”到“主动协同”的转变。

## 3. 交互与体验（UX）规范

### 3.1 消除命令行传参负担
**当前问题**：CLI 需要频繁传递 `--project` 参数或依赖环境变量配置，没有工作区（Workspace）的概念。
**重构规范**：
- **本地项目缓存**：引入 `.agentxin/workspace.json` 或隐式工作区锁定机制，使得在项目目录内执行命令时自动推断上下文，像使用 `git` 命令一样自然流畅。

### 3.2 可视化进度与流式输出 (Streaming)
**当前问题**：大模型生成大纲或正文耗时极长，CLI 执行时终端处于“盲等”卡死状态，缺乏反馈。
**重构规范**：
- **实时输出**：所有耗时的生成任务必须接入流式（Streaming）API 响应。
- **进度追踪**：在终端引入进度条（如 tqdm 或 cli-progress），在 Web UI 明确显示当前节点（如“正在生成大纲 3/10”或“正在进行逻辑一致性检查”）。

### 3.3 优雅的进程管理
**当前问题**：通过 `start.bat` 强行拉起两个堵塞的 CMD 窗口，极其粗暴且容易造成端口被残留占用。
**重构规范**：
- 采用单进程管理工具（如 `pm2`、`concurrently`）或打包成桌面端应用（Electron / Tauri），实现后台静默运行、统一日志输出和进程生命周期的优雅退出（Graceful Shutdown）。

## 4. 数据与版本控制机制

### 4.1 安全写入与人工介入 (Human-in-the-loop)
**当前问题**：生成结果通常以覆盖写入（`--overwrite`）的方式处理，错误生成会导致原稿丢失。
**重构规范**：
- **非破坏性修改**：默认开启草稿/修订机制。所有修改不直接覆盖原文件，而是生成 `.draft` 文件或提供 Diff 视图，必须经过人工确认（Approve）才能合入主文。
- **版本历史**：建立轻量级的本地版本控制机制，让用户随时可以回退到上一个“干净”的章节版本。

### 4.2 友好的 Mock 与鉴权回退策略
**当前问题**：在 API Key 缺失时，系统会静默回退至 Mock 模式并在文件中生成 `MOCK_OUTPUT`，容易对用户造成困扰。
**重构规范**：
- **显式拦截**：如果检测不到有效的鉴权配置，并且用户未显式开启 `--mock` 标志，系统必须立即报错中止运行，并给出配置引导，绝对禁止未经许可的静默脏数据写入。

## 5. 跨平台与编码兼容性

### 5.1 强制字符集约束
**当前问题**：在 Windows 默认编码环境下，Python CLI 会出现 `С˵Ŀ...` 等严重的中文乱码问题。
**重构规范**：
- **编码声明**：所有涉及文件读写、标准输入输出的代码，必须强制使用 `utf-8` 编码。Python 代码中应使用 `open(..., encoding='utf-8')`，并在 CLI 入口针对 Windows 平台动态调整 `sys.stdout.encoding`，确保多语种环境下的稳健性。

### 5.2 统一相对路径与日志输出 (Relative Paths)
**当前问题**：CLI 提示信息及内部路径处理经常使用绝对路径（如 `C:\Users\...\projects\...`），这不仅造成终端日志极其冗长，也不利于项目在不同设备间迁移和共享。
**重构规范**：
- **终端友好输出**：所有面向用户输出的成功提示（如 `wrote ...` 或 `[OK] Output: ...`），必须转换为相对当前执行目录（CWD）或工作区根目录的**相对路径**（Relative Path）。
- **配置与存储的相对化**：确保存储在配置（如 `workspace.json`）中的路径指针，以及项目内互相引用的路径，一律使用相对路径，彻底解耦项目与本地文件系统的绝对依赖，实现“开箱即运行”、“即拷即用”。
